version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: subscription-service-mongo
    restart: unless-stopped
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: subscription-service
    volumes:
      - mongodb_data:/data/db
    networks:
      - subscription-network

volumes:
  mongodb_data:
    name: subscription-service-mongodb-data

networks:
  subscription-network:
    driver: bridge
